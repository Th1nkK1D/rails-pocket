<div class="flex flex-col bg-blue-600 text-white rounded-xl shadow py-3 px-4 space-y-2">
  <div class="flex-1 flex flex-row text-xs">
    <div class="flex-1"><%= @timestamp %></div>
    <button data-action="click->transaction#remove" data-id="<%= @id %>">[x]</button>
  </div>
  <div class="flex flex-row">
    <div class="flex-1 text-2xl font-bold">
      <%= @name %>
    </div>
    <div class="text-2xl">฿<%= @amount %></div>
  </div>
</div>